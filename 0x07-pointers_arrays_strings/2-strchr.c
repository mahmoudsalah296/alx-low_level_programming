#include <stdio.h>
#include "main.h"

/**
 * _strchr - locates a character in a string.
 * @s: the string
 * @c: the character
 * Return: pointer to first occurence
 */

char *_strchr(char *s, char c)
{
	return (s);
}
