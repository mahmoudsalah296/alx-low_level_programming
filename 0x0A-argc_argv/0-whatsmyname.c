#include <stdio.h>

/**
  * main - entry point
  * @argc: arg count
  * @argv: poiter to args
  * Return: 0 if success
  */
int main(__attribute__ ((unused))int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
